name: odoid
description: Deterministic mixed-radix ID encoding — maps integers to 6, 7, or 8-character alphanumeric strings. Ambiguous characters I, L, O excluded.
version: 1.0.2
homepage: https://github.com/Webictbyleo/odoid
repository: https://github.com/Webictbyleo/odoid
issue_tracker: https://github.com/Webictbyleo/odoid/issues

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
